- Settings → Updates has Check now, so a new release shows up right away instead of after the next daily check.
- A household's page can be closed from the Android app by holding the top-left corner for three seconds.
- A faint mark appears in that corner after one second: keep holding. Taps there still reach the page.
- Whoever runs a server can keep a household's page in the server's own files, instead of editing it by hand.

Calendars, lists, photos and settings from 0.1.4 carry over as they are. Closing the page from the corner needs the
0.1.5 app; with an older one, Back and a few quiet minutes still bring the wall back.
